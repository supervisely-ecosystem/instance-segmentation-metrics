показывать кликнутую аннотацию

неправильные значения cls_report (cm_categories)
    recall все равно не тот (потмоу что он где-то 0)

убрать хардкоды в globals
test compute_overlaps.pyx

потом еще созвонится с Максом перед релизом

---
показывать кликнутую аннотацию

наведение на картинки?

add progress_bar
в per-class можно тоже фильтровать аннотации (show all anns)

---
стресс тесты:
    разные меты (классы)
    разные картинки и датасеты 
    что если в пред нет conf?
    если в гт есть класс, а в пред нет, то мы все равно должны матчить
    разные шейпы (polygon)